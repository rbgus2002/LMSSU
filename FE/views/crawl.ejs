<!DOCTYPE html>
<html>
  <head>
    <title>Hello</title>
    <link rel="stylesheet" type="text/css" href="styles/index.css"/>
  </head>
  <body>
    <div>
      <%= messageList.length %>
      <input class="red" type="button", value="createDiv" onclick="createDiv()"></input>
      <input type="button", value="test" onclick="test()"></input>
      <iframe src = "hello/messages" title = "messages"></iframe>
      <iframe src = "hello/lectures" title = "lectures"></iframe>
    </div>
    <script>

      function test() {
        const newDiv = document.createElement('div');
        const text = document.createTextNode('hello');
        newDiv.appendChild(text);
        document.body.append(newDiv);
      }

      let load = false;
      
      function testCalc1() {
        let result = 0;
        for(let i = 0; i < 1000000000; i++) {
          result++;
        }
        load = true;
        console.log(result);
        return result;
      }
      function testCalc2() {
        let result = 0;
        for(let i = 0; i < 2000000000; i++) {
          result++;
        }
        load = true;
        console.log(result);
        return result;
      }

      function createDiv() {
        console.log('hello');
      }
    </script>
  </body>
</html>